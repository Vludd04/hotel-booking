import React from "react";

const CitiesListCols = ({ children }) => {
  return (
    <div className="mt-6 justify-between lg:flex text-center">{children}</div>
  );
};

export default CitiesListCols;
